# AI 학습 도우미 — 배포 단계

현재 코드 상태
==============
이 폴더는 Streamlit Community Cloud에 올릴 수 있도록 정리된 배포용 버전입니다.

구조:
사용자 브라우저
→ Streamlit Community Cloud
→ OpenAI API
→ 구술 질문 / 채점 / 취약점 분석 / 맞춤 문제

따라서 배포가 끝나면 개발자 맥북을 켜둘 필요가 없습니다.

--------------------------------------------------
1. OpenAI API 쪽
--------------------------------------------------

현재 API 계정에 크레딧이 없으면 웹사이트 자체는 배포할 수 있지만
"질문 생성" 등 실제 AI 버튼은 동작하지 않습니다.

실제 시연을 하려면 OpenAI API 계정에 사용 가능한 크레딧이 필요합니다.

API Key는 app.py에 직접 적지 마세요.

--------------------------------------------------
2. GitHub 저장소
--------------------------------------------------

Streamlit Community Cloud는 GitHub 저장소를 연결해 배포합니다.

새 저장소를 만든 뒤 이 폴더의 다음 파일/폴더를 올립니다.

app.py
ai_engine.py
document_engine.py
storage.py
requirements.txt
README.md
.streamlit/config.toml
.gitignore

절대 올리지 말 것:
.streamlit/secrets.toml
.env
API Key
.venv 폴더

--------------------------------------------------
3. Streamlit Community Cloud
--------------------------------------------------

share.streamlit.io 에서:

Create app
→ GitHub repository 선택
→ Branch: main
→ Main file path: app.py
→ Deploy

--------------------------------------------------
4. API Key를 서버에 숨겨 넣기
--------------------------------------------------

배포 화면의 Advanced settings 또는
배포 후 App settings → Secrets에 아래 형식으로 입력합니다.

OPENAI_API_KEY = "실제_API_KEY"
OPENAI_MODEL = "gpt-5.6-luna"
MAX_AI_CALLS_PER_SESSION = 30

저장 후 앱을 재시작하면 사이드바에:

"AI 서버 연결됨"

이라고 표시됩니다.

이제 방문자는 API Key를 입력하지 않아도 됩니다.

--------------------------------------------------
5. 최종 사용
--------------------------------------------------

배포가 성공하면:

https://원하는이름.streamlit.app

형식의 주소가 생깁니다.

친구:
링크 접속
→ 강의자료 업로드
→ 기출 업로드
→ 자료 분석
→ 구술시험
→ 답변
→ AI 채점
→ 취약점 분석
→ 맞춤 문제

개발자 맥북:
꺼져 있어도 됨

--------------------------------------------------
6. 발표용 권장 설정
--------------------------------------------------

- 공개 URL은 기억하기 쉬운 이름 사용
- MAX_AI_CALLS_PER_SESSION = 20~30 정도
- 강의자료 PDF 1개 + 기출 1개로 데모 준비
- 일부러 구술 답변에서 핵심 하나를 빠뜨려 취약점 탐지 시연
- 이어서 맞춤 계산문제 또는 개념문제가 생성되는 모습 시연

--------------------------------------------------
7. 주의
--------------------------------------------------

서버 API Key를 사용하면 방문자가 AI 기능을 사용할 때
배포자 OpenAI API 계정의 사용량이 발생합니다.

프로토타입 공개 테스트 전에는
API 사용 한도/결제 한도를 낮게 설정하고 링크 공유 범위를 제한하는 것을 권장합니다.

SQLite 파일은 Community Cloud의 영구 사용자 DB로 쓰기에는 적합하지 않습니다.
현재 버전은 프로토타입이므로 세션 중심으로 사용하세요.
향후 사용자 계정/영구 학습기록이 필요하면 Supabase 등의 외부 DB를 붙이는 것이 좋습니다.
